package me.abacate.animefoda.enums

enum class StateAnimelist(val label: String) {
    watching("watching"),
    completed("completed"),
    on_hold("on_hold"),
    dropped("dropped"),
    plan_to_watch("plan_to_watch"),
}
